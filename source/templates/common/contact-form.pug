.call.blur-form__wrapper
    .blur-form
    .call__content
        .subtitle.call__title
            | связаться со мной
        .call-fields
            mixin inputs(type, placeholder, id)
                    input(type=type placeholder=placeholder id=`auth-input--${id}` name=id).call-fields__field
            form.call-fields__form
                +inputs('text','Имя', 'name')
                +inputs('text','Email', 'email')
                textarea(placeholder='Ваше сообщение', id='auth-input--mess' name='mess' rows='5').call-fields__field

            mixin btns(href, name)
                a(href=href class='btn-nav__link')= name
            .call-nav.btn-nav
                +btns('#','Отправить')
                +btns('#','Очистить')
